from django.urls import path
from . import views
from django.urls import include, path

urlpatterns = [
    # path('addRatings/', views.addRatings, name='add_ratings'),  # /companies/add/
    # path('getRatings/', views.getRatings, name='add_company'),  # /companies/add/
    # Add other company-related URLs here (e.g., get company, update company)
]